#include "../server.hpp"

namespace Carbonide { namespace Server
{
//#####################################################################################################################
    void CarbonideServer::setupUserCalls()
    {
        app_.get("/api/user/authenticate", [this](auto req, auto res)
        {
            res->send_status(200);
        });

        //app_.get()
    }
//---------------------------------------------------------------------------------------------------------------------
//#####################################################################################################################
}
}
